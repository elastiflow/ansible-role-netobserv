# netobserv/defaults/main.yml
---
# Default version of NetObserv Flow Collector
netobserv_version: "7.4.0"

# URLs for Debian-based distributions
netobserv_package_deb: "flow-collector_{{ netobserv_version }}_linux_amd64.deb"

# URLs for RHEL-based distributions
netobserv_package_rpm: "flow-collector-{{ netobserv_version }}-1.x86_64.rpm"

# GPG key information
netobserv_trust_string: "6A2E26EFDE24AA7A634A442ED5C0572E5D212F6B:6:"
